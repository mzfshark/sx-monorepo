{
  "version": 2,
  "builds": [
    { "src": "package.json", "use": "@vercel/static-build", "config": { "distDir": "apps" } }
  ],
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "apps " : "/index.html" }
  ]
}
